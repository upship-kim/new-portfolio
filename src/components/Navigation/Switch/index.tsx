import React from 'react';
import styled from 'styled-components';

const index = () => {
    return <Container>dark/light mode switch</Container>;
};

const Container = styled.div`
    text-align: center;
`;
export default index;
